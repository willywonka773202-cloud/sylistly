node_modules
.next
/out/
.env
.env.local
.env.*.local
.DS_Store
*.log
.vercel
.turbo
coverage
*.tsbuildinfo

.claude/
.image-masters/

# auto-ingestion staging output (retailer data — never commit; promote via Higgsfield pipeline)
data/ingested-staging.json

# catalog data backups (redundant, regenerable)
data/*.backup-*.json
